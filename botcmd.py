# Commands on which to call `execcmd`
concmd = []

# Executed on every message
# `line` is message split on ' '
# irc:
#     irc.send(line)           Send `line` over IRC
#     irc.msg(chan/nick, msg)  Send `msg` using PRIVMSG to `chan/nick`
#     irc.nick                 The nick the bot was started with
#     irc.chan                 The channels the bot was started with
def parse((line, irc)):
	return

def execcmd(cmd):
	return
